/* =========================================================
   PURCHASE MANAGEMENT PAGE STYLES (BookNest)
   ========================================================= */

/* ====== GLOBAL RESET ====== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}

body {
  background: #f5f6fa;
  color: #222;
  overflow-x: hidden;
}

/* ====== HEADER (Breadcrumb) ====== */
.header {
  background: #fff;
  padding: 15px 25px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
  margin-left: 260px;
  border-radius: 8px;
  margin-top: 60px;
  flex-wrap: wrap;
  gap: 10px;
  position: relative;
  z-index: 2;
}

.breadcrumb {
  display: flex;
  align-items: center;
  font-weight: 500;
  color: #555;
  font-size: 15px;
  gap: 8px;
}

.breadcrumb i {
  color: #ff7b00;
  font-size: 17px;
}

/* ====== ADD BUTTON ====== */
.btn-primary {
  max-width: 300px;
  background: linear-gradient(135deg, #ff7b00, #ff9800);
  border: none;
  color: #fff;
  padding: 10px 18px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 15px;
  transition: 0.3s ease;
  box-shadow: 0 4px 10px rgba(255, 123, 0, 0.25);
  margin: 20px 25px 10px 260px;
}

.btn-primary:hover {
  background: linear-gradient(135deg, #ff9800, #ff7b00);
  transform: scale(1.05);
}

/* ====== POPUP / INVOICE MODAL ====== */
.popup-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.4);
  display: none;
  justify-content: center;
  align-items: center;
  z-index: 999;
  padding: 15px;
}

.popup-content {
  background: #fff;
  width: 100%;
  max-width: 900px;
  border-radius: 12px;
  padding: 30px 40px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  position: relative;
  animation: fadeIn 0.3s ease-in-out;
  overflow-y: auto;
  max-height: 90vh;
}

.popup-content h2 {
  text-align: center;
  font-weight: 700;
  margin-bottom: 5px;
}

.popup-content h3 {
  text-align: center;
  font-weight: 600;
  margin-bottom: 25px;
  color: #333;
}

.close-btn {
  position: absolute;
  top: 15px;
  right: 20px;
  font-size: 20px;
  color: #777;
  cursor: pointer;
  transition: 0.2s;
}

.close-btn:hover {
  color: #ff7b00;
  transform: rotate(90deg);
}

/* ====== INVOICE SECTIONS ====== */
.invoice-section {
  margin-top: 20px;
}

.invoice-section h4 {
  border-bottom: 1px solid #ddd;
  padding-bottom: 6px;
  font-weight: 600;
  color: #222;
  margin-bottom: 10px;
}

table {
  width: 100%;
  border-collapse: collapse;
  table-layout: fixed;
}

td {
  padding: 8px 5px;
  vertical-align: top;
  word-wrap: break-word;
}

td b {
  color: #333;
}

.total {
  font-weight: 700;
  text-align: right;
  margin-top: 20px;
  font-size: 18px;
}

/* ====== PRINT BUTTON ====== */
.print-btn {
  display: block;
  margin: 25px auto 5px;
  padding: 10px 25px;
  background-color: orange;
  color: white;
  border: none;
  border-radius: 6px;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s ease;
}

.print-btn:hover {
  background-color: rgb(234, 109, 0);
}

/* ====== MAIN CONTENT ====== */
.content {
  margin: 10px 25px 25px 260px;
  background: #fff;
  border-radius: 10px;
  padding: 25px;
  box-shadow: 0 3px 8px rgba(0, 0, 0, 0.08);
  transition: 0.3s ease;
}

/* ====== TOOLBAR ====== */
.toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 18px;
  flex-wrap: wrap;
  gap: 12px;
}

.tools-left a,
.tools-left button {
  color: #ff7b00;
  font-weight: 500;
  text-decoration: none;
  font-size: 14px;
  margin-right: 15px;
  transition: 0.3s;
  display: inline-flex;
  align-items: center;
  background: none;
  border: none;
  cursor: pointer;
}

.tools-left i {
  margin-right: 6px;
}

.tools-left a:hover {
  color: #cc6500;
}

.tools-right input {
  padding: 8px 14px;
  border-radius: 6px;
  border: 1.5px solid #ccc;
  width: 230px;
  font-size: 14px;
  transition: 0.3s ease;
}

.tools-right input:focus {
  border-color: #ff7b00;
  box-shadow: 0 0 6px rgba(255, 123, 0, 0.4);
  outline: none;
}

/* ===== FILTER POPUP ===== */
.filter-popup {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 300px;
  padding: 22px;
  border-radius: 16px;
  background: rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(12px) saturate(180%);
  -webkit-backdrop-filter: blur(12px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.35);
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  z-index: 100;
  animation: fadeIn 0.3s ease;
  color: orange;
}

.filter-popup select {
  width: 100%;
  padding: 10px 12px;
  margin-bottom: 12px;
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.15);
  color: white;
  border: 1px solid rgba(255, 255, 255, 0.5);
}

.filter-popup option {
  color: black;
}

.filter-popup-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 14px;
}

.filter-popup-header strong {
  font-size: 1.1rem;
  color: #ff7b00;
}

.filter-popup .apply-btn {
  width: 100%;
  padding: 10px;
  border-radius: 10px;
  border: none;
  background: linear-gradient(135deg, #ff7b00, #ff9933);
  color: #fff;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s ease;
}

.filter-popup .apply-btn:hover {
  transform: scale(1.05);
}

/* ===== TABLE WRAPPER & TABLE ===== */
.table-container {
  width: 100%;
  overflow-x: auto;
  overflow-y: auto;
  max-height: 65vh;
  border-radius: 10px;
  border: 1px solid #eee;
  background: white;
}

.purchase-table {
  width: 100%;
  border-collapse: separate !important;
  border-spacing: 14px 10px; /* Column & Row Gap */
  min-width: 850px;
}

.purchase-table th,
.purchase-table td {
  text-align: left;
  padding: 14px 16px;
  font-size: 14px;
  background: #fff;
  border-bottom: 1px solid #eee;
  white-space: nowrap;
  border-radius: 6px;
}

.purchase-table th {
  background: #fff3e6 !important;
  font-weight: 600;
  color: #333;
  position: sticky;
  top: 0;
  z-index: 10;
}

.purchase-table tr:hover td {
  background: #fff2e0;
}

.purchase-table i {
  font-size: 18px;
  cursor: pointer;
  transition: 0.3s;
}

.purchase-table .fa-file-invoice { color: #ff9800; }
.purchase-table .fa-edit { color: #ff7b00; margin-right: 10px; }
.purchase-table .fa-trash { color: #b347cc; }
.purchase-table i:hover { transform: scale(1.1); }

/* ===== MODAL BASE ===== */
.modal-overlay,
.delete-modal-overlay,
#purchaseModal {
  display: none;
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0,0,0,0.45);
  backdrop-filter: blur(8px);
  justify-content: center;
  align-items: center;
  z-index: 9999;
  padding: 15px;
}

/* Modal Box */
.modal-box,
.delete-modal-box,
#purchaseModal .modal-content {
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: 16px;
  width: 90%;
  max-width: 600px;
  padding: 25px 30px;
  color: #fff;
  backdrop-filter: blur(15px) saturate(180%);
  box-shadow: 0 8px 40px rgba(0,0,0,0.4);
  animation: fadeIn 0.3s ease;
}

/* Modal Header */
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1.5px solid rgba(255,255,255,0.2);
  margin-bottom: 15px;
  padding-bottom: 8px;
}

.modal-header h3 {
  font-size: 20px;
  font-weight: 600;
}

/* Modal Body */
.modal-body {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 15px 20px;
  margin-bottom: 20px;
}

.modal-body textarea {
  grid-column: 1 / -1;
  resize: none;
  height: 80px;
}

.modal-body label {
  font-weight: 500;
  color: #eee;
  font-size: 14px;
}

.modal-body input,
.modal-body select,
.modal-body textarea {
  width: 100%;
  padding: 9px 10px;
  border: 1px solid rgba(255,255,255,0.3);
  border-radius: 8px;
  font-size: 14px;
  background: rgba(255,255,255,0.1);
  color: #fff;
  transition: 0.3s ease;
}

.modal-body input:focus,
.modal-body select:focus,
.modal-body textarea:focus {
  border-color: #ff7b00;
  box-shadow: 0 0 10px rgba(255,123,0,0.4);
}

/* Modal footer buttons */
.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  border-top: 1px solid rgba(255,255,255,0.2);
  padding-top: 15px;
}

.modal-footer .btn,
.delete-modal-footer .cancel-btn,
.delete-modal-footer .ok-btn {
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s;
  font-size: 14px;
  border: none;
}

.modal-footer .update {
  background: linear-gradient(135deg, #7b00ff, #a855f7);
  color: #fff;
}

.modal-footer .update:hover {
  background: linear-gradient(135deg, #a855f7, #7b00ff);
}

.modal-footer .cancel {
  background: transparent;
  border: 1.5px solid #ff7b00;
  color: #ff7b00;
}

.modal-footer .cancel:hover {
  background: #ff7b00;
  color: #fff;
}

.delete-modal-footer .ok-btn {
  background: linear-gradient(135deg, #ff0000, #ff7b00);
  color: #fff;
}

/* ===== ANIMATION ===== */
@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}





/* ===== RESPONSIVE ===== */
@media (max-width: 992px) {
  .header,
  .btn-primary,
  .content { margin-left: 0; width: 100%; }

  .toolbar { flex-direction: column; align-items: flex-start; }

  .tools-right input { width: 100%; }

  .popup-content { width: 95%; padding: 20px; }
}

@media (max-width: 768px) {
  .purchase-table th,
  .purchase-table td { font-size: 13px; padding: 10px 8px; }

  .table-container { max-height: 60vh; }
}

@media (max-width: 480px) {
  .modal-box { padding: 20px; }
  .modal-body { grid-template-columns: 1fr; }
}
